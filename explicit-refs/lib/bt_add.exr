
(* Add exercise 3 *)

let n_left = { data <= 12; left <= 0; right <= 0}    (* 0 in head signals null *)
in let n_right = { data <= 44; left <= 0; right <= 0}
in let n_root = { data <= 33; left <= n_left ; right <= n_right }
in let t1 = { root <= n_root ; length <= 3} 
in letrec helper(nd) = proc(key) {
  if number?(nd) then {data <= key; left <= 0; right <= 0}
  else if nd.data << key 
        then begin 
              nd.right <= ((helper nd.right) key);
              nd
            end
        else if nd.data >> key
              then begin 
                    nd.left <= ((helper nd.left) key);
                    nd
              end
        else nd  
}
in let add_bt = proc(t) {proc(key) {
  begin
  t.root <= ((helper t.root) key);
  0
  end
}}
in begin
     ((add_bt t1) 23);
     t1.length <= t1.length+1;
     debug(t1)		
   end